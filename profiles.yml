jaffle_shop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: "{{env_var('AIRFLOW_POSTGRES_PASSWORD_VALUE')}}"
      port: 5432
      dbname: postgres
      schema: public
      threads: 6
